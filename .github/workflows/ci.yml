name: CI

on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
      - 'specs/**'
      - '.specify/**'
  push:
    branches:
      - main
  workflow_dispatch:

# A newer push to the same branch makes the in-flight run irrelevant.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # 8.5 is what vercel-php@0.9.0 runs in production; 8.4 is what the
        # local toolchain uses. Both are tested so neither can drift unnoticed.
        php: ['8.4', '8.5']
    name: validate (PHP ${{ matrix.php }})
    # Deliberately no DB, cache, session or queue settings here. A job-level
    # `env` becomes a real environment variable, and phpunit.xml's <env> only
    # fills in values that are not already set — so anything named here would
    # silently outrank it and the test run would use different infrastructure
    # from the one developers run locally. That divergence is what made two
    # content tests fail only in CI: phpunit.xml asks for an in-memory
    # database, CI handed them a file one that a RefreshDatabase case had
    # already emptied. The artisan steps below get their connection from .env,
    # which the prepare step writes from .env.example.
    env:
      APP_ENV: testing
      APP_DEBUG: false
      APP_URL: http://127.0.0.1:8088
      ADMIN_SEED_EMAIL: ''
      ADMIN_SEED_PASSWORD: ''
      ADMIN_SEED_NAME: ''
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, dom, fileinfo, sqlite3, pdo_sqlite, intl
          coverage: none

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm

      - name: Install Composer dependencies
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: Install Node dependencies
        run: npm ci

      - name: Prepare environment
        run: |
          cp .env.example .env
          mkdir -p database
          touch database/database.sqlite
          php artisan key:generate --force
          php artisan migrate --force
          php artisan db:seed --force

      # These three gates existed as npm scripts but were never run here, so
      # they silently rotted. They run before the slower steps so a formatting
      # or typing regression fails fast.
      - name: Check formatting
        run: npm run format:check

      - name: Lint
        run: npm run lint:check

      - name: Check PHP formatting
        run: composer run lint:check

      - name: Run frontend type checks
        run: npm run types:check

      - name: Build frontend assets
        run: npm run build

      - name: Run backend tests
        run: php artisan test

      - name: Verify routes
        run: php artisan route:list
