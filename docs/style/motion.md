# Motion

Motion is deliberately restrained in v0.

- subtle hover lifts on cards
- no parallax
- no decorative autoplay motion
- full `prefers-reduced-motion` fallback in CSS

The deeper motion system belongs to `005-theme-and-motion`.
