# Tokens

Current visual tokens are CSS custom properties in `resources/css/app.css`.
They cover:

- background surfaces
- border strengths
- ink and muted text colors
- green and red accents
- a soft shadow for frosted panels
- a sans + serif font pair

This is enough for v0. A later theme spec can extract them into a more formal design-token map.
