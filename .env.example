APP_NAME="Ismael Rodmacq"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8088
SITE_PUBLIC_URL=http://127.0.0.1:8088
SITE_URL_PLACEHOLDER="{{site_url}}"

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4
BCRYPT_ROUNDS=12

LOG_CHANNEL=stderr
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
DB_DATABASE=database/database.sqlite
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=cookie
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync

CACHE_STORE=file
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"
SITE_SETTINGS_SOURCE=files
SITE_NAME="Ismael Rodmacq"
SITE_TAGLINE="Full-stack e-commerce. Cross-functional data, reliable flows."
SITE_DESCRIPTION="Full-stack web development for e-commerce, product-data flows, CMS delivery, consent-aware tracking, and technical SEO."
SITE_AUTHOR_NAME="Ismael Rodmacq"
SITE_AUTHOR_TITLE="Full-stack e-commerce developer for product data, integrations, tracking, and technical SEO"
SITE_CONTACT_EMAIL="ismael@rodmacq.com"
SITE_SAME_AS="https://github.com/Ismaouste,https://www.linkedin.com/in/ismaelrodmacq"
ADMIN_SEED_EMAIL="admin@sidewalk-studio.test"
ADMIN_SEED_NAME="Sidewalk Admin"
ADMIN_SEED_PASSWORD="change-me-local"
CONSENT_MODE=strict
# Allowed: none, umami, vercel. Drivers load only after explicit opt-in via the consent modal.
ANALYTICS_DRIVER=none
# Umami driver — public website id (UUID) and self-hosted script origin.
UMAMI_WEBSITE_ID=
UMAMI_SCRIPT_URL=
# Vercel driver — Speed Insights is perf-only and stays always-on; Web Analytics is gated by consent.
VERCEL_ANALYTICS_ENABLED=false
CONSENT_COOKIE_NAME=sidewalk_consent

# Build-time toggles consumed by vite.config.ts (not read by PHP).
# VERCEL=1 is injected automatically on Vercel builds; SKIP_WAYFINDER_GENERATE=1 skips wayfinder route generation locally.
# VERCEL=
# SKIP_WAYFINDER_GENERATE=
